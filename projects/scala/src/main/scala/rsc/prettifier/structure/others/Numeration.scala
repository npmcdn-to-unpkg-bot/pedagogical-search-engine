package rsc.prettifier.structure.others

import rsc.prettifier.structure.Structure

case class Numeration(numbers: List[Int],
                      original: List[String],
                      oText: Option[String])
  extends Structure {

}
